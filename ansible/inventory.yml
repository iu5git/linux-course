all:
  children:
    # Группа web
    web:
      children:
        # Группа stage
        stage:
          vars:
            env: stage
          hosts:
            ansible_host: 127.0.0.1
            ansible_port: 22
        # Потенциальная группа prod
        # prod: ...
